#!/bin/sh
# M22 S1 fixture helper. Materialized into the container at
# /data/skills/runnable-helper/scripts/greet.sh; proves a skill's scripts/
# reach the sandbox and are executable.
echo "runnable-helper: hello from a materialized skill script"
