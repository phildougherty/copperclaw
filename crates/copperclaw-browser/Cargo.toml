[package]
name = "copperclaw-browser"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
copperclaw-container-rt = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
tracing = { workspace = true }
url = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
reqwest = { workspace = true }
# Same version + TLS backend the discord adapter already pins, so the CDP
# WebSocket transport reuses a dependency tree already in the lock.
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
base64 = "0.22"
copperclaw-metrics.workspace = true

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
